package com.faceAI.demo.base.utils

import com.bumptech.glide.annotation.GlideModule
import com.bumptech.glide.module.AppGlideModule

/**
 * Glide 相关配置
 *
 */
@GlideModule
class MyAppGlideModule : AppGlideModule() {
    // 暂时留空即可
    // 以后你可以在这里配置缓存大小、自签名证书 HTTPS 等
}